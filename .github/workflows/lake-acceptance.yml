name: lake-acceptance

on:
  pull_request:
    types: [labeled]
  workflow_dispatch:
    inputs:
      expected_sha:
        description: "Exact 40-character commit SHA under acceptance"
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: lake-acceptance
  cancel-in-progress: false

jobs:
  actual-r2:
    if: >-
      ${{
        github.event_name == 'workflow_dispatch' ||
        (github.event_name == 'pull_request' &&
         github.event.action == 'labeled' &&
         github.event.label.name == 'lake-acceptance-approved' &&
         github.actor == github.repository_owner &&
         github.event.pull_request.head.repo.full_name == github.repository)
      }}
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # A dispatched acceptance run names an exact SHA that need not be the event
          # ref, and a shallow clone can simply not contain it. Fetching every ref of
          # this repository is what makes "check out the SHA the evidence is attributed
          # to" reliable, and it also proves the SHA belongs here rather than to a fork.
          fetch-depth: 0
          persist-credentials: false

      - name: Validate exact head without credentials
        id: validate-input
        env:
          EXPECTED_DISPATCH_SHA: ${{ inputs.expected_sha }}
          EXPECTED_PR_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          if [[ "$GITHUB_EVENT_NAME" == "workflow_dispatch" ]]; then
            EXPECTED_SHA="$EXPECTED_DISPATCH_SHA"
          elif [[ "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
            EXPECTED_SHA="$EXPECTED_PR_SHA"
          else
            echo "lake acceptance does not support this event" >&2
            exit 2
          fi
          if [[ ! "$EXPECTED_SHA" =~ ^[0-9a-f]{40}$ ]]; then
            echo "expected_sha must be a full lowercase commit SHA" >&2
            exit 2
          fi
          if ! git cat-file -e "$EXPECTED_SHA^{commit}" 2>/dev/null; then
            echo "expected_sha is not a commit in this repository" >&2
            exit 2
          fi
          if [[ "$(git rev-parse HEAD)" != "$EXPECTED_SHA" ]]; then
            git checkout --detach "$EXPECTED_SHA"
          fi
          test "$(git rev-parse HEAD)" = "$EXPECTED_SHA"
          echo "expected_sha=$EXPECTED_SHA" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version-file: .python-version

      - uses: astral-sh/setup-uv@ae62891fec2bb8e7d6c99fc78c9fec3a63790f8d # v10.0.0
        with:
          enable-cache: true

      - name: Sync locked environment
        run: uv sync --locked

      # Only the jobs that read the lake over HTTP install the extension, and they
      # install it before credentials are present. Runtime sessions disable extension
      # downloads and load this exact-version copy, so a job that never opens a remote
      # lake must not inherit the extension repository as a failure dependency.
      - name: Provision DuckDB httpfs extension
        run: uv run python -m tools.diagnostics.provision_duckdb_httpfs

      - name: Run actual R2 acceptance
        env:
          BAIBAI_R2_ACCEPTANCE: "1"
          R2_ACCOUNT_ID: ${{ vars.R2_ACCOUNT_ID }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_LAKE_ACCEPTANCE_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_LAKE_ACCEPTANCE_SECRET_ACCESS_KEY }}
          R2_LAKE_ACCEPTANCE_BUCKET: ${{ vars.R2_LAKE_ACCEPTANCE_BUCKET }}
          PYTHONPATH: engine/src:batch/src:web/backend/src
        run: uv run pytest -q -n 0 tests/integration/test_lake_r2_acceptance.py
