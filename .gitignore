# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~
__pycache__/
*.pyc

# Python build and quality-tool outputs
.coverage
.coverage.*
.mypy_cache/
.ruff_cache/
dist/

# Local planning drafts
.plan/

# Screening caches
# Disposable local cache for provider byproducts such as EDINET CSV ZIPs.
.cache/

# Weekly screen output (candidates). Deterministic L2 output over the SQLite
# store; history is kept on disk as a local store (same durability posture as
# data/screening/market.sqlite) and is not tracked in git.
# See docs/components/candidates.md §2.
records/02-candidates/**/*.yaml

# Canonical application database and consistent manual snapshots
data/app/

# Local environment files
.env

# Local Codex workspace files
/.codex

# local design-review HTML deliverables (kept locally, not committed)
/baibai-loop-*.html
